export default class Size {
    /**
     * @param {Number} width 
     * @param {Number} height 
     */
    constructor(width, height) {
        /**@type {Number} */
        this.width = width;
        /**@type {Number} */
        this.height = height;
    }
}